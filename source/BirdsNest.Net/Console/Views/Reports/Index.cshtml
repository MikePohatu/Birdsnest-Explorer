@using Console.Plugins


@{
    ViewData["Title"] = "Reports";
    Layout = "_Layout_NonSpa";
}

@{
    foreach (Plugin plugin in PluginManager.Plugins.Values)
    {
        @await Html.PartialAsync("PluginReportListPartial", plugin);
    }
}